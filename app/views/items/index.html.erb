<div class="container">
  <div class="row">
    <div>
      <h2>User: <%= current_user.email %> </h2>
    </div>

    <div class="col-md-6 adding">
      <%= render 'items/form', locals: { item: @item } %>
    </div>

    <div class="col-md-6 to-do">
      <%= render partial: 'items/item', locals: { item: @item } %>
    </div>
  </div>
<!-- 1st div -->
</div>
